set(main, [] {
    set(x, open("HtTp://checkip.dyndns.org/"))
    save({
        for({
            write(stdout, read(x))
        })
    }, e: {
        if( (e~/past the end/) {
            set(e, "saved")
        })
    })
})
main()
