set(main, [x] {
    set(h, [x]{
        set(g, [x]{
            set( f, [x]{
                write(stdout, (x=3))
            })
            f(x)
        })
        g(x)
    })
    h(x)
})
set(x, 3)
main(x)
