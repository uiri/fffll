Set(x, [[3, 2, 1], [4, 5, 6]])
Set(i, 0)
Set(l, Len(x))
While( (i<l) {
    Set(m, Len(x.[i]))
    Set(j, 0)
    While ((j<m) {
        Write(stdout, x.[i].[j])
        Set(j, Add(j, 1))
    })
    Set(i, Add(i, 1))
})
Write(stdout, x.[x.0.2].[x.0.1])
Set(x.0.1, 7)
Set(y, [0, 2, 5, 3, 7, 8])
Write(stdout, y.-3)
Write(stdout, x)
