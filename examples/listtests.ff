set(main, [] {
    set(x, [4, 2, 6, 4.6, 10])
    write(stdout, head(x))
    write(stdout, tail(x))
    write(stdout, tail(x))
    set(x, tail(x))
    write(stdout, head(x))
    write(stdout, tail(x))
    write(stdout, tail(x))
    push(x, 5)
    write(stdout, x)
    write(stdout, head(x))
    write(stdout, tail(x))
    set(y, 3)
    set(x, [y, x])
    -- inf loop -- write(stdout, x.1)
})
main()
