Def(main, [], {
    Set(x, [4, 2, 6, 4.6, 10])
    Write(stdout, Head(x))
    Write(stdout, Tail(x))
    Write(stdout, Tail(x))
    Set(x, Tail(x))
    Write(stdout, Head(x))
    Write(stdout, Tail(x))
    Write(stdout, Tail(x))
    Push(x, 5)
    Write(stdout, x)
    Write(stdout, Head(x))
    Write(stdout, Tail(x))
})
Main()